# Implementing a MEAN STACK on AWS Cloud

I deployed a Book Register web app build with MEAN stack to AWS Cloud.

The following were the steps I took:

1. Install Node.js

2. Install MongoDB

3. Install Express and set up routes to the server

4. Access the routes with AngularJS


The full documentation is in `project4.md`. 
